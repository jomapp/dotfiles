- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - ./scripts/setup_oh_my_posh.bash

